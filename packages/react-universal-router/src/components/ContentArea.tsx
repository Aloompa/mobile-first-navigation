import * as React from 'react';

import { View } from 'react-native';

const ContentArea = () => (
    <View style={{
        flex: 1,
        position: 'relative'
    }} />
);

export default ContentArea;