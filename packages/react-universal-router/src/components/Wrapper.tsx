import * as React from 'react';

import { View } from 'react-native';

const Wrapper = () => (
    <View style={{
        flex: 1,
        position: 'fixed',
        height: '100vh',
        width: '100vw'
    }} />
);

export default Wrapper;